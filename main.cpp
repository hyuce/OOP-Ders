#include <string>
#include <map>
#include <iostream>
#include<cstdlib>

using namespace std;



template<class Compare>
void bubble_sort(BidirectionalIterator first,
                 BidirectionalIterator last, Compare comp){ BidirectionalIterator left = first , right = first ;
    right ++ ;
    while( first != last){
    } }
while( right != last ){
if( comp(*right,*left) )
iter_swap(left,right) ; right++ ;
left++;
}
last -- ;
left = first ; right = first ;

int main() {

}
